# Frontend (Flutter)

This directory is reserved for the Flutter multi-platform client (`android`, `ios`, `web`, `windows`, `macos`, `linux`).

Initialize it on a machine with Flutter installed:

```bash
flutter create frontend
```
